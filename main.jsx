import React from "react";
import ReactDOM from "react-dom/client";
import LuxuryPortfolio from "../LuxuryPortfolio.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <LuxuryPortfolio />
  </React.StrictMode>
);
